# ---------- Prefect deployment ----------

# To create and run a deployment use the following commands:
#   cd path/to/rs-demo
#   prefect deploy --prefect-file prefect_deployment/prefect.yaml
# Follow the steps of this command
# If in local mode, lauch a worker in a separated terminal
#   prefect worker start --pool 'kubewp1'
# Lauch the deployment:
#   prefect deployment run 'staging/staging_deployment'

# Generic metadata about this project
name: prefect_hello_world_deployment
prefect-version: 3.1.4

pull:
- prefect.deployments.steps.git_clone:
    id: clone-step
    repository: https://github.com/RS-PYTHON/rs-demo.git
    branch: feat-rspy516/prefect-deployment-dask-cluster

# - prefect.deployments.steps.pip_install_requirements:
#     directory: "{{ clone-step.directory }}/prefect_deployment"
#     requirements_file: requirements.txt
- prefect.deployments.steps.run_shell_script:
    id: test
    directory: "{{ clone-step.directory }}/prefect_deployment"
    script:
      pip install --force-reinstall -r requirements.txt

deployments:
- name: staging_deployment
  entrypoint: ./prefect_deployment/flows/hello_world.py:dask_cluster
  schedules:
    - cron: "0 * * * *"
      timezone: "Europe/London"
      active: true
  flow_name: hello_world
  parameters: {}
  work_pool:
    name: staging-pool
    job_variables:
      env:  
        DASK_GATEWAY__AUTH__TYPE: "jupyterhub"
        JUPYTERHUB_API_TOKEN: "{{ prefect.blocks.secret.api-token-block }}"
        DASK_GATEWAY__ADDRESS: "{{ prefect.blocks.secret.dask-gateway-address-block }}"
